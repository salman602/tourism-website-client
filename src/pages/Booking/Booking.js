import React from 'react';
import { useParams } from 'react-router';

const Booking = () => {
    
    return (
        <div>
            <h4>This is booking page.</h4>
        </div>
    );
};

export default Booking;